framework:
    messenger:
        transports:
            # Laravel Cloud Managed Queue (SQS via the in-container cloud-agent).
            # Set MESSENGER_TRANSPORT_DSN to sync:// in .env.local to run without SQS.
            async: '%env(MESSENGER_TRANSPORT_DSN)%'

        routing:
            'App\Message\ProcessJob': async
