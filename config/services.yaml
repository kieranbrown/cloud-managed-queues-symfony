parameters:
    # Build-time fallback so cache:clear/warmup doesn't fail before Laravel Cloud
    # injects the real APP_SECRET (the committed .env isn't read at build time).
    env(APP_SECRET): ''

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
